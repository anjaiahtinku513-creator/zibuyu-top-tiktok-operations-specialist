# 参与贡献

欢迎贡献市场本地化、品类规则、服务适配、易用性改进与问题修复。通过 Issue 描述场景、预期行为和实际结果，提交聚焦该问题的 Pull Request。

## 验证与提交

- 工作台：进入 `workbench`，执行 `npm ci`、`npm test` 与 `npm run lint`。
- 插件：在根目录执行 `python -m unittest discover -s tests`；受影响技能若有独立测试目录，也运行相应测试。
- 发布和恢复逻辑使用离线夹具验证，不在测试中触发真实生成或发布。
- 文档修改检查路径与当前实现一致；打包内容变更时更新 ZIP 和 SHA-256。

不提交凭据、登录信息、私有账号数据、用户素材、视频、台账、授权回执、日志、依赖目录或构建产物。示例使用虚构数据，明确已验证和未验证部分。

保留未知提交查证、防重复、实际成片验收和平台回读边界，不通过删除校验让失败任务显示成功。PR 说明问题、最终行为、验证和限制。贡献者应有权提交内容，贡献按本仓库 MIT 许可证提供，不要求转让版权。
