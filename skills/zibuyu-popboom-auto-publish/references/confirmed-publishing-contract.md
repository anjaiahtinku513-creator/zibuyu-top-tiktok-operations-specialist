# Confirmed PopBoom publishing contract

Contract ID: `popboom_beijing_caption_v1`. Confirmed by the user on 2026-09-11 after successful M4D785-EU and N4Z118-EU scheduling workflows. This is the user's standing operational convention for this PopBoom integration, not newly claimed provider documentation.

- `publish_video.video_title` is exactly the delivered market-language caption followed in the same paragraph by exactly five unique relevant non-brand hashtags. Do not create a separate short title, drop text/tags, invent a length limit, or repeatedly request proof of this mapping. Preserve live API rejections; never truncate silently.
- Interpret the requested date and default `07:00 / 12:00 / 18:00` in the account's configured IANA zone. Resolve the actual date-specific DST offset, convert the resulting instant to `Asia/Shanghai`, and always submit `scheduled_time` with explicit `+08:00`. Convert the calendar date too. Germany uses `Europe/Berlin`; US account bindings currently use `America/New_York`. Never assume every European/US account shares a timezone: use its configured binding.
- For genuine `check_publish` schedule readbacks of these Beijing submissions, a bare `YYYY-MM-DD HH:MM:SS` is interpreted as Beijing time under this contract. Preserve the raw value and record this contract as the timezone basis, then compare UTC instants. Explicit offsets in responses take precedence. Do not infer from the computer zone, replace readback with expected values, or accept wrong clocks. Missing time, malformed time, negative-zero offset, identity/copy mismatch or unknown submission still requires review. A missing offset alone is no longer a blocker or another user question.
- User-confirmed whole-batch manual acceptance is valid final QA (`keep`, basis `user_manual_acceptance`); retain earlier automated limitations and evidence rather than claiming the agent independently verified them.
- When the user explicitly requests publishing an identified accepted batch and supplies/reuses account, PID and date, persist a concrete manifest and bind that request as authorization. Choose the normal slots and color order without another confirmation of these defaults. Ask only for genuinely missing/ambiguous input, a changed scope not authorized, or a concrete mismatch; not a second approval of the same request.
- Preserve duplicate protection, exact PID/channel lookup, future-time checks, per-account allocations, original action IDs, and query each accepted schedule. A timeout is `submission_unknown`, never an automatic retry. Scheduling and actual publication remain different states. Changes to skills do not authorize new live posts or rewrite historical receipts.

Example: 2026-09-15 Berlin `07:00 / 12:00 / 18:00` -> Beijing `2026-09-15 13:00 / 18:00` and `2026-09-16 00:00`. New York winter 07:00 -> Beijing 20:00, summer 07:00 -> Beijing 19:00.

For `audit_schedule_time.py`, set `platform_contract: "popboom_beijing_caption_v1"` and `submitted_time` to the actual +08:00 request. `expected_time` may remain account-local with its valid DST offset. The helper applies this convention only to `check_publish` evidence and an equivalent +08:00 submission; other platforms remain explicit-evidence-only.
