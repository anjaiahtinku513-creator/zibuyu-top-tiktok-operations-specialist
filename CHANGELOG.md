# 2026-09-07

插件：`0.1.0+codex.20260907023455`。

## 插件

- 主流程、PopBoom、导演采用短技能入口与按阶段引用，已有任务可先查询状态、恢复质检或补交付。
- 新增 `inspect_run.py` 只读快照，检查父子范围、版本与缺失文件；快照不能代替生产校验或授权。
- 新增 `--summary --result-file`，完整结果原子落盘，终端仅返回摘要、位置和哈希；保护输入、已有编译文件与授权相关文件。
- 新增整批发布交接校验和可单独使用的发布技能，保留市场证据、真实成片 QA、明确付费与发布授权。
- 发布文案要求五个不同的相关非品牌标签，禁止 `#Imily Bela` 与 `#ImilyBela`。

## 工作台

- 首次将已在本机使用的工作台源码纳入 `workbench/`。
- 多选固定模特，按颜色与模特组合创建独立制作单并归为同一批；不同市场的来源、语言、模特身份和授权分别绑定。
- 原图独立复制并核对 SHA-256；请求 ID 防止网络重试重复创建制作单；中断恢复保留已有生产或授权证据。
- 发布中心展示整批交付、文案与排期意向；同批模特全部验收交付后才能进入准备，真实正文传输未验证时仍阻断提交。
- 每次 CLI 尝试记录阶段、原始用量、耗时与可识别工具次数。缺失值保持未知，不推算跨会话 Token 总额或节省比例。
- 玻璃导航、顶部栏与底部固定进度 Dock，保留清晰内容区和减少动画、透明度的降级样式。
- 移除颜色识别输出 schema 中不兼容的 `uniqueItems`，图片归组唯一性继续由本地校验保证。
- 记录本次 CLI 终止错误，区分非致命插件警告，避免显示旧错误或仅显示退出码；修复重复的 50 字符颜色名造成的死循环。

## 验证

- 插件：104 项 Python 测试，103 通过，1 项因 Windows 环境无法创建符号链接而跳过。
- 工作台：46 项测试通过，TypeScript、lint 与生产构建通过。
- 颜色识别：六张原始服装图只读复测返回三组、每组两张，无遗漏与重复，原文件 SHA-256 不变。
- 源码同步逐文件校验哈希，安装包解压内容与插件源文件一致。

此次验证没有生成付费视频或发布 TikTok 内容。工作台仍采用每个制作单独立的 `gpt-5.5 / high` 会话，付费续跑仅使用该制作单自己的会话。
